[tool.vercel]
entrypoint = "api.search:app"

[project]
name = "hybrid-search-engine"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "ddgs>=9.14.0",
    "fastapi>=0.117.0",
    "httpx>=0.27.0",
]
